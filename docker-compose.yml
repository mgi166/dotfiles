version: '3'
services:
  # gentoo:
  #   image: gentoo/stage3-amd64
  #   expose:
  #     - "22"
  serverspec:
    build:
      context: .
      dockerfile: Dockerfile.serverspec
    expose:
      - "22"
    volumes:
      - .:/serverspec
    links:
      - ubuntu
  ubuntu:
    build:
      context: .
      dockerfile: Dockerfile.ubuntu
    expose:
      - "22"
